---
title: 考研笔记-数据结构-8.排序
author: caterpillar
pubDatetime: 2024-04-21T00:00:00Z
featured: false
draft: false
tags:
  - 考研笔记 408
  - 数据结构
description: 数据结构 排序
---

# 考研笔记-排序

- `冒泡、选择、堆、快速排序 每趟都有一个元素被放在最终位置上`
- `n个关键字排序的比较次数至少为`$\log_2{n!}$

## 插入排序 `移动次数和初始序列有关` 需要挪位 移动次数多

### 直接插入排序 `依赖初始状态 稳定 时间复杂度 O(n^2) 比较次数 （有序时）O(n)（逆序时）O(n^2)`

### 折半插入排序 `稳定 时间复杂度 O(n^2) 比较次数O(nlogn)`

### \*希尔排序 `不稳定 时间复杂度 O(n^2) 比较次数O(nlogn)`

（相隔某个增量）组成一个子表，分割成若干子表，对子表进行直接插入排序

## 交换排序 `排序趟数和初始序列有关` 只交换 移动次数少

### 冒泡排序 `依赖初始状态 稳定 时间复杂度 O(n^2) 比较次数O(n^2)`

### 快速排序 `依赖初始状态 不稳定 空间复杂度O(logn) 时间复杂度 好(无序)O(nlogn) 差（基本有序/逆序时）O(n^2) 比较次数O(n^2)`

基于递归 需要栈 最小空间复杂度 O(logn) 最大 O(n)

## 简单选择排序 `不稳定 时间复杂度 O(n^2) 比较次数(不分好坏)O(n^2) 移动次数 O(n)`

每次从待排序中选择最值 交换

## 堆排序 `不稳定 时间复杂度 O(nlogn) 建堆O(n) 每次调整O(h)`

- 大根堆（根结点最大）
- 小根堆（根结点最小）
  从[1...n], 索引采用向下取整

## 归并排序 `稳定 时间复杂度 O(nlogn) 空间复杂度O(n)`

趟数m = [$\log_{k路}元素个数N$]

## 基数排序 `排序躺数固定d(位数) 稳定 时间复杂度 O(d(n+r)) 空间复杂度O(n)`

`不基于 比较和移动`

- MSD 最高位优先 （高->低）
- LSD 最低位优先

## 外部排序

- 排序文件较大 内存无法一次装入
- 一般采用归并排序法

尽量减少归并趟数（减少i/o次数），即增加归并路数k（每次多处理一些），但内部归并时间增加（远小于i/o时间）

对`r`个初始归并段, 做`k`路归并，采用严格k叉树。
数的高度h - 1 = [$\log_{k}r$] = 归并躺数S

### 多路平衡归并

- 在n个元素中选择关键字最小的记录需要比较`n-1`次

### 败者树（完全2叉树）`减少关键字对比次数` 归并次数与k无关

k路归并的败者树深度 = [$\log_2k$]
总比较次数 = (n-1)[$\log_2{r(初始归并段个数)}$]

### 置换-选择排序 `减少初始归并段r数量` 得到长度不等的初始归并段

总记录个数n，每个归并段的长度l，则归并段的个数 r = [n\l]

待排序input，内存工作区Mem(可以装入w个记录), MIN 记录当前最小值

1. 从 input 中选取 w个记录放入工作区 Mem中
2. 从Mem中选取 最小值m 并且 大于 MIN， 并将 MIN = m, m放入output中，并从input中选取一个记录放入 Mem中 （选取最小值的过程 使用败者树）
3. 重复第二步 直至 Mem中选不出新的MIN时。此时得到一个初始归并段 output，并重置MIN

### 最佳归并树 `WPL最少`

优化树的带权路径长度WPL (I/O次数)，m叉树的哈夫曼树（严格m叉树）

#### 补充虚段个数 = k - 1 - (r-1) % (k-1)
